#include <iostream>
#include <studio.h>
#include <conio.h>

#define WHITE 0
#define GREY 1
#define BLAK 2

using namespace std;

int n , e;
int capacity[100][100], flow[100][100], color[100];
int head, tail;
int q[102];

bool dfs(int head, int tail){
    int u, v;
    for(u=0; u<n; u++)
		color[u] = WHITE;
	head = 0;
	tail = 0;
	enque(start)

}
  int main(){

    return 0;
}
